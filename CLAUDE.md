# 🧠 ذاكرة المشروع — نظام سند (Sanad App)

> **هذا الملف هو نقطة الدخول الوحيدة للمشروع.**
> يُقرأ تلقائياً في كل جلسة عمل جديدة (Claude Code)، وهو أول ما يجب أن يقرأه
> أي إنسان يفتح المشروع لأول مرة — على ويندوز أو ماك أو أي جهاز.
>
> **آخر تحديث: 2026-08-23**

---

## ⚡ الملخص في ثلاثين ثانية

**سند** نظام إدارة خزائن ومحاسبة نقدية لشركة مقاولات ومشاريع عراقية.
يدير: السنوات المالية · الخزائن · سندات القبض والصرف والتحويل · سلف المشاريع
المستوردة من إكسل · الموظفين والرواتب · المقاولين والشركاء · التقارير · النسخ
الاحتياطي المشفَّر · سجل تدقيق كامل.

**التقنيات:** Flutter · Drift (SQLite) · Riverpod · go_router · Material 3
**المنصة الهدف:** ويندوز سطح المكتب · **الواجهة:** عربية RTL بالكامل
**الحالة:** 230/230 اختبار ناجح · المحلّل 0 مشاكل · Schema v5

### المبدأ الحاكم الذي يقوم عليه كل التصميم
> **الأرصدة لا تُخزَّن أبداً.** تُحسَب في كل مرة من الـ VIEW
> `v_treasury_balances` مباشرةً من السندات. لهذا لا يمكن **بنيوياً** أن
> يتناقض رقمان في البرنامج. لا تكسر هذه القاعدة مهما بدا الأمر مغرياً.

---

## 📚 خريطة التوثيق — ابدأ من هنا

| الملف | متى تقرأه |
|---|---|
| [docs/01-OVERVIEW.md](docs/01-OVERVIEW.md) | **أول مرة تفتح المشروع** — ما هو البرنامج، لمن، وكيف يتدفّق المال فيه |
| [docs/02-CODEMAP.md](docs/02-CODEMAP.md) | **قبل كتابة أي كود** — خريطة كل ملف وما يفعله، وقائمة الكود الميت الذي لا تبنِ عليه |
| [docs/03-RULES.md](docs/03-RULES.md) | **قبل أول تعديل** — القوانين الصارمة الملزمة: الاختبار والتحقق والتعليقات العربية |
| [docs/04-STATUS.md](docs/04-STATUS.md) | **لمعرفة أين وصلنا** — ما أُنجز وما تبقّى وترتيب التنفيذ |
| [docs/05-LESSONS.md](docs/05-LESSONS.md) | **عند مواجهة أي عطل** — كل مشكلة واجهناها وكيف حُلَّت، دروس دائمة |
| [docs/06-SETUP.md](docs/06-SETUP.md) | **لتشغيل المشروع** — على ويندوز أو ماك أو الويب، من الصفر |
| [AGENTS.md](AGENTS.md) | ميثاق التطوير المشترك بين المالك والمطوّر |
| [plan/PROGRESS_LOG.md](plan/PROGRESS_LOG.md) | السجل التاريخي المفصّل لكل جلسة عمل |

---

## 🚨 القواعد الخمس التي لا تُكسَر

هذه ملخّص [docs/03-RULES.md](docs/03-RULES.md) — اقرأه كاملاً قبل العمل.

1. **بعد كل تعديل:** `flutter analyze` = 0 مشاكل · `flutter test` = كل
   الاختبارات ناجحة ولا ينخفض عددها أبداً عن **230**.
2. **كل إصلاح عطل يحتاج اختبار إثبات** يفشل قبل الإصلاح وينجح بعده.
   إن تعذّر، أثبت العطل في اختبار مؤقّت ثم احذفه ووثّق النتيجة.
3. **تعليقات عربية شاملة** في كل ملف ودالة ومنطق أعمال — تشرح **لماذا** لا
   **ماذا**. الكود يقول ماذا، والتعليق يقول لماذا اختير هذا الحلّ دون غيره.
4. **الحارس يوضع في أعمق طبقة ممكنة** (DAO أو Repository) لا في الواجهة.
   *حارس لا يمرّ به اختبار ليس حارساً.*
5. **حدِّث التوثيق قبل إنهاء أي مهمة:** [docs/04-STATUS.md](docs/04-STATUS.md)
   و[docs/05-LESSONS.md](docs/05-LESSONS.md) و[plan/PROGRESS_LOG.md](plan/PROGRESS_LOG.md)
   بالتاريخ. توثيق غير محدَّث أسوأ من لا توثيق لأنه يكذب بثقة.

---

## 🔥 المحاذير — أخطاء وقعت فعلاً ولا يجوز تكرارها

| المحذور | لماذا |
|---|---|
| ❌ لا تُنشئ `TextEditingController` ثم تتخلّص منه بعد `await showDialog` | الـ await ينتهي لحظة `Navigator.pop` لا لحظة اختفاء الحوار → عطل شاشة حمراء. استعمل متغيّراً نصّياً مع `initialValue`/`onChanged`. يحرسه اختبار آلي. |
| ❌ لا تستعمل الإزاحة البتّية `1 << 32` | تساوي **صفراً** على الويب (أعداد JavaScript ٣٢-بت) → `RangeError`. استعمل `0xFFFFFFFF`. |
| ❌ لا تُزح حدود التواريخ بـ `Duration(days: 1)` لمحاكاة `<=` | تُزيح ٨٦٬٤٠٠ ثانية لا لحظة → رفضت كل سنة مالية مجاورة. استعمل `isSmallerOrEqualValue` مباشرةً. |
| ❌ لا تستعمل `.replace()` على صفوف Drift عند التحديث الجزئي | يُعيد الحقول الغائبة لقيمها الافتراضية فيُحيي المحذوف ويمسح `created_at`. استعمل `.write()`. |
| ❌ لا تحذف صفاً أباً قبل أبنائه | `PRAGMA foreign_keys = ON` مُفعَّل — الترتيب إلزامي. |
| ❌ لا تظنّ أن نجاح الاختبارات يعني أن الميزة تعمل | 185 اختباراً نجحت بينما قاعدة التقاطع معطوبة كلياً، لأنها كانت في طبقة العرض. |

التفاصيل الكاملة لكل محذور — بسببه وأثره وإصلاحه — في [docs/05-LESSONS.md](docs/05-LESSONS.md).

---

## 🏃 تشغيل سريع

```bash
flutter pub get
```
```bash
dart run build_runner build --delete-conflicting-outputs
```
```bash
flutter run -d windows
```

على VS Code: اضغط **F5** — الإعداد الافتراضي هو ويندوز.
التفاصيل الكاملة (ماك، الويب، بناء الإنتاج) في [docs/06-SETUP.md](docs/06-SETUP.md).
