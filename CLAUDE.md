# 🧠 ذاكرة المشروع — نظام سند (Sanad App)

> **هذا الملف هو نقطة الدخول الوحيدة للمشروع.**
> يُقرأ تلقائياً في كل جلسة عمل جديدة، وهو أول ما يجب أن يقرأه أي إنسان
> يفتح المشروع لأول مرة — على ويندوز أو ماك أو أي جهاز.
>
> **آخر تحديث: 2026-09-01**

---

## ⚡ أين نحن الآن — الملخص في ثلاثين ثانية

**سند** نظام إدارة خزائن ومحاسبة نقدية لشركة مقاولات ومشاريع عراقية.

| | |
|---|---|
| **الحالة** | ✅ **754/754 اختبار ناجح** · المحلّل **0 مشاكل** · Schema **v7** |
| **الفرع** | `main` — مدموج ومرفوع بالكامل |
| **المنصة الهدف** | **ويندوز سطح المكتب** (الويب يعمل لكن لا نستثمر فيه) |
| **الواجهة** | عربية RTL بالكامل |
| **المخطَّط المعتمد** | ✅ **مكتمل** — المراحل أ · ب · ج · د كلها منجزة |
| **الجاري** | ✅ **١٦ مرحلة + دفعتا ملاحظات** — لا عطل معروف مفتوح |
| **⚠️ الآن** | 📋 **بقيت دفعتان من ملاحظات المالك (ج · د) وسؤالان ينتظران جوابه** — راجع «الخطوة التالية» |

**التقنيات:** Flutter · Drift (SQLite) · Riverpod · go_router · Material 3

### المبدأ الحاكم الذي يقوم عليه كل التصميم
> **الأرصدة لا تُخزَّن أبداً.** تُحسَب في كل مرة من الـ VIEW
> `v_treasury_balances` مباشرةً من السندات. لهذا لا يمكن **بنيوياً** أن
> يتناقض رقمان في البرنامج. لا تكسر هذه القاعدة مهما بدا الأمر مغرياً.

---

## 📚 خريطة التوثيق — ابدأ من هنا

| الملف | متى تقرأه |
|---|---|
| [docs/01-OVERVIEW.md](docs/01-OVERVIEW.md) | **أول مرة تفتح المشروع** — ما هو البرنامج، لمن، وكيف يتدفّق المال فيه |
| [docs/02-CODEMAP.md](docs/02-CODEMAP.md) | **قبل كتابة أي كود** — خريطة كل ملف، والأنماط الإلزامية، والكود الميت |
| [docs/03-RULES.md](docs/03-RULES.md) | **قبل أول تعديل** — القوانين الثمانية الملزمة |
| [docs/04-STATUS.md](docs/04-STATUS.md) | **لمعرفة أين وصلنا** — ما أُنجز وما تبقّى |
| [docs/05-LESSONS.md](docs/05-LESSONS.md) | **عند مواجهة أي عطل** — كل مشكلة واجهناها وسببها الجذري وإصلاحها |
| [docs/06-SETUP.md](docs/06-SETUP.md) | **لتشغيل المشروع** من الصفر على أي جهاز |
| [AGENTS.md](AGENTS.md) | ميثاق التطوير المشترك بين المالك والمطوّر |
| [plan/PROGRESS_LOG.md](plan/PROGRESS_LOG.md) | السجل التاريخي المفصّل لكل جلسة عمل |

---

## 🗺️ رحلة المشروع — ماذا أنجزنا وكيف

كل مرحلة كشفت أعطالاً **لم تكن في الخطة**. التفاصيل الكاملة في
[05-LESSONS.md](docs/05-LESSONS.md) و[PROGRESS_LOG.md](plan/PROGRESS_LOG.md).

| المرحلة | ما أُنجز | أبرز ما كُشِف |
|---|---|---|
| **تدقيق أمني** | ٦ ثغرات حرجة (CRIT-1…6) | قفل الحساب **معطَّل كلياً** · سجل التدقيق **كود ميت** |
| **سلامة مالية** | معاملات ذرّية · حرّاس الرصيد والفترات | سباق تسديد السلف · حذف التحويلات |
| **سلف المشاريع** | Schema v5 · المسودة لا تمسّ الدفاتر | — |
| **الثغرات الست** | ح-١…ح-٦ | تعديل التحويل **يخلق مالاً من العدم** · `1 << 32` = صفر على الويب |
| **أ** | ث-١…ث-٣ | زرّ التصفير **لم يعمل قط** · تعديل السند **بلا أثر تدقيق** |
| **ب-١** | حقول التتبّع والفلاتر | المستودع **يُسقط ٤ حقول في ٣ مواضع** · قوائم بنود ثابتة تُعطّل ميزة |
| **ب-٢** | تقريرا المصروفات والمستحقات | ٣ قرارات محاسبية محروسة باختبارات |
| **ب-٣** | الشعار · ترويسة PDF · التسمية | الشعار **يُكتَب ولا يُقرأ قط** |
| **إصلاح عاجل** | العربية في PDF | كل نصّ عريض بـ **Helvetica-Bold بلا حرف عربي** |
| **ج** | المرفقات · Schema v6 | **`onUpgrade` لم يُختبَر قط** في تاريخ المشروع |
| **د** | الدين التقني | ٨٤٠ سطراً مكرّراً · ١٦٤ شرط ثيم · ٣ ملفات عملاقة |
| **ملاحظات المالك** | ٨ بنود | **٦ منها أعطال حقيقية**: التوقيت UTC · مؤشّر لا يتوقّف · زر رجوع معطّل |
| **الرواتب ١–٤** | كشف الشهر · الاستيراد · الربط بالسلف · الطباعة والتقرير | الترحيل ينظّف الماضي و**الكاتب الحيّ بقي يُنتج العطل** (ع-٢٨) |
| **الرواتب ٥** | توحيد الصرف المباشر داخل الكشوف · تقرير الموظف | حارسٌ صحيح صار **يقفل الشهر** بعد أن تغيّر ما حوله (ع-٢٩) |
| **الرواتب ٦** | التصحيح بعد التسديد · تفسير الفرق · إغلاق ثغرة السند | حذف سند الرواتب يُبقي الراتب «مصروفاً» **بلا مال** (ع-٣١) |
| **الرواتب ٧** | حذف الكشف بخيارين · السندات اليتيمة · الحذف من البطاقة | **العطل الثالث من الفئة نفسها**: كل باب حذفٍ يعرف مكاناً ويجهل الباقي (ع-٣٣) |
| **الرواتب ٨** | تغطية سطر السلفة = الكشف كلّه · تنبيه حذف الخزينة | ربطُ مالٍ بمالٍ عبر **حقلٍ إداريّ** يجعل الحارس يبدو كاذباً (ع-٣٤) |
| **السلف ٩** | سند واحد بدل سند لكل سطر · إلغاءٌ يسحب الرواتب | **الحارس لا يحمي من كاتبٍ لا يمرّ به** (ع-٣٦) · ومساران للتسديد أحدهما ينسى الأقساط (ع-٣٧) |
| **سلف الموظفين ١٠** | حارس سند السلفة · الإلغاء · تأكيد كلمة المرور | حذف السند يُبقي الدَّين (ع-٣٨) · وسندُ قبضٍ لمالٍ **لم يتحرّك** (ع-٣٩) |
| **التصحيح ١١** | إلغاء تسديد الشهر · الكاشف المرآة | **عمودٌ واحد بمعنيين** حذف رواتب شهرٍ كامل — وتعليقٌ لم يُتحقَّق منه (ع-٤٠) |
| **تصفير المصنع ١٢** | زرٌّ يمحو التطبيق كلّه بكلمة مرور ورمز محو | **نسخُ ترتيب الحذف مرّتين** هو عين العلّة التي أنتجت ستة أعطال — فالمسار واحد |

**الاختبارات: ١٧٦ ← ٧٥٤** · Schema **v5 ← v7** · المحلّل **0** طوال الطريق.

---

## 🚨 القوانين الثمانية التي لا تُكسَر

ملخّص [docs/03-RULES.md](docs/03-RULES.md) — اقرأه كاملاً قبل العمل.

1. **بعد كل تعديل:** `flutter analyze` = 0 · `flutter test` = كل الاختبارات
   ناجحة، والعدد **لا ينخفض أبداً** عن **754**.
2. **كل إصلاح عطل يحتاج اختبار إثبات** يفشل قبل الإصلاح وينجح بعده.
3. **تعليقات عربية شاملة** تشرح **لماذا** لا **ماذا**، وبتاريخ العطل.
4. **الحارس في أعمق طبقة ممكنة** (DAO أو Repository) لا في الواجهة.
   *حارس لا يمرّ به اختبار ليس حارساً.*
5. **حدِّث التوثيق قبل إنهاء أي مهمة** — توثيق غير محدَّث أسوأ من لا توثيق.
6. **موافقة المالك المسبقة** على: أي تبعية جديدة · تغيير معماري · تغيير
   Schema · حذف أي ملف أو ميزة.
7. **التشغيل الفعلي لا يُغني عنه اختبار** — `flutter run -d windows`.
8. **الحذف الناعم مقدَّس** — الاستثناء الوحيد المحو القسري بثلاث طبقات حراسة.

---

## 🔥 المحاذير — أخطاء وقعت فعلاً ولا يجوز تكرارها

| المحذور | لماذا |
|---|---|
| ❌ لا تُنشئ `TextEditingController` ثم تتخلّص منه بعد `await showDialog` | الـ await ينتهي لحظة `Navigator.pop` لا لحظة اختفاء الحوار → شاشة حمراء. يحرسه اختبار آلي. |
| ❌ لا تمرّر `base` وحده إلى `pw.ThemeData.withFont` | الخط العريض يبقى Helvetica-Bold بلا عربية — `copyWith(x: null)` لا يمحو الافتراضي. املأ الأربعة. |
| ❌ لا تمرّر `DateTime.now()` مفتاحاً لمزوّد عائلي | تتغيّر كل ميلي ثانية فيُنشأ مزوّد جديد في كل بناء ولا ينتهي التحميل. طبّعها إلى يوم. |
| ❌ لا تعرض `created_at` بلا `.toLocal()` | تُملأ بـ `CURRENT_TIMESTAMP` وهي **UTC** — تظهر ناقصة ٣ ساعات ببغداد. |
| ❌ لا تكتب `isDark ? xDark : xLight` | استعمل `context.colors.x`. نسيان طرف لا يشتكي منه المحلّل — نصّ أسود على خلفية سوداء. |
| ❌ لا تنسخ ودجتاً لتغيّر لونه | مرّر اللون معاملاً. ٨٤٠ سطراً مكرّراً بين شاشتَي السند نشأت هكذا. |
| ❌ لا تُصدر ترحيل مخطط بلا اختبار يُشغّل `onUpgrade` | `onCreate` يبني من عدم، و`onUpgrade` يلمس بيانات لا تُعوَّض. |
| ❌ لا تُخزّن مسار ملف **مطلقاً** | تغيّر حرف القرص يكسر كل الروابط. خزّن نسبياً بفاصل `/`. |
| ❌ لا تستعمل الإزاحة البتّية `1 << 32` | تساوي **صفراً** على الويب → `RangeError`. استعمل `0xFFFFFFFF`. |
| ❌ لا تُزح حدود التواريخ بـ `Duration(days: 1)` لمحاكاة `<=` | تُزيح ٨٦٬٤٠٠ ثانية لا لحظة → رفضت كل سنة مالية مجاورة. |
| ❌ لا تستعمل `.replace()` على صفوف Drift عند التحديث الجزئي | يُحيي المحذوف ويمسح `created_at`. استعمل `.write()`. |
| ❌ لا تحذف صفاً أباً قبل أبنائه | `PRAGMA foreign_keys = ON` — الترتيب إلزامي. |
| ❌ لا تختبر ملفاً بأنه «صالح» فقط | ملف PDF سليم شكلياً وفارغ معنىً يمرّ من كل فحص لا ينظر إلى محتواه. |
| ❌ لا تقرأ حارساً بمعزل عمّا حوله | حين يتغيّر **كيف** تُبلَغ حالةٌ ما، يصير الحارس يحرس شيئاً آخر: صرفُ راتب واحد جعل كشف الشهر «مُسدَّداً» فأقفله على ٤٧ موظفاً (ع-٢٩). |
| ❌ لا تقارن `created_at` بعمودٍ يكتبه كودك | الأول تملؤه القاعدة بدقّة **الثانية**، والثاني بالميلي ثانية — فالمقارنة تكذب داخل الثانية الواحدة (ع-٣٠). |
| ❌ لا تُضِف جدولاً يشير إلى جدول قديم بلا مراجعة حرّاس القديم | المفتاح الأجنبي يحمي من الحذف الصلب لا **الناعم**: حذف سند الرواتب كان يُبقي الراتب «مصروفاً» بلا مال (ع-٣١). |
| ❌ لا تمنع عمليةً بلا مسار بديل محروس | المستخدم سيجد طريقاً آخر — والمنع بلا بديل يُهجّر الخطر إلى باب بلا حارس لا يُزيله (ع-٣٢). |
| ❌ لا تستنتج الأثر المالي من حالة مُشتقّة | `status` يصف الغالب، والمال يعيش في **كل صفّ على حدة**: كشفٌ «مسودة» كان فيه ٢٬٦٥٠٬٠٠٠ مصروفة، فحُذفت بصمت (ع-٣٣). |
| ❌ لا تُصلح باب حذفٍ واحداً وتترك البقية | العملية المالية تعيش في ثلاثة جداول، وكل باب يعرف واحداً: ثلاثة أعطال متتالية من الفئة نفسها (ع-٢٨ · ع-٣١ · ع-٣٣). |
| ❌ لا تربط مالاً بمالٍ عبر حقلٍ إداريّ مشتقّ | `employees.treasury_id` يتغيّر بالنقل والحذف والاستيراد، فصار كشفٌ يساوي سطر سلفته **يُرفض** بحجّة عدم التطابق (ع-٣٤). اربط بما اختاره الإنسان صراحةً. |
| ❌ لا تكتب `await ref.read(provider.future)` في الواجهة | المزوّدات المولَّدة `autoDispose` و`read` لا يضيف مستمعاً، فيُتخلَّص منها قبل وصول الجواب — **سباقٌ** ينجح مرّة ويُسقط التطبيق مرّة (ع-٣٥). استعمل `ref.readOnce`. |
| ❌ لا تظنّ الحارس يحمي جدولاً | يحمي **الدالة** التي وُضع فيها: `cancelAdvance` كتب `UPDATE` مباشرةً فمرّ من حول حارس السندات (ع-٣٦). ابحث عن كل من يكتب في الجدول لا عن الباب الذي أمامك. |
| ❌ لا تكرّر منطقاً مالياً في مسارين | التسديد من خزينة كان يسجّل أقساط السلف، والتسديد عبر سلفة **ينساها** — فيُخصَم من الموظف ولا يُحسَب له (ع-٣٧). |
| ❌ لا تُنشئ سنداً لنيّةٍ لم تتحرّك | «خصم من الراتب» كان يُنشئ سند قبض لمالٍ لم يدخل، ثم يُخصم من الراتب فيُحتسب **مرّتين** (ع-٣٩). السند يمثّل حركةً وقعت لا نيّةً ستقع. |
| ❌ لا تبنِ على تعليقٍ لم تتحقّق منه | تعليقٌ يقول «هذه الحالة مستحيلة» يُقنع القارئ التالي ولو كان كاتبه: إلغاء سلفة موظف حذف **رواتب الشهر كلّه** بسبب تعليقٍ كاذب (ع-٤٠). |
| ❌ لا تقرأ حقلاً يحمل معنيين بلا شرطه | `voucher_id` في القسط: سندُ قبضٍ خاصّ به إن كان نقدياً، وسندُ رواتب الشهر إن كان خصماً من الراتب (ع-٤٠). |
| ❌ لا تقرأ مزوّداً بـ`ref.read(p).valueOrNull` بلا `ref.watch` له في المكتبة نفسها | المزوّدات المولَّدة `autoDispose`، فالقراءة تُنشئه وتُعيد `null` **دائماً** — بصمت. عطّلت النسخةَ الشاملة كلياً (ع-٤١) وترويسةَ كل سند (ع-٤٢). استعمل `ref.readOnce` أو اقرأ من الـDAO. |
| ❌ لا تكتب عقوبةً مؤقّتة بلا اختبارٍ لما **بعد** انقضائها | قفل الحساب كان يصير دائماً: العدّاد لا يُصفَّر بانقضاء المدّة، فخطأٌ واحد بعدها يُعيد القفل فوراً (ع-٤٣). |
| ❌ لا تجعل خطوة تنظيف تُفشل عمليةً تمّت | `close()` في Drift لا ينتظر تحرير مقبض الملف (isolate منفصل)، فحذف `-wal` بعد دهس القاعدة رمى خطأً على **استعادة ناجحة** فبدت فاشلة (ع-٤٤). رتّب ما يفشل **قبل** ما لا رجعة فيه. |
| ❌ لا تفترض أن `pw.Table` تعرف الاتجاه | حزمة `pdf` **بلا أي إشارة إلى `Directionality`** — ترتّب الأعمدة من اليسار دائماً، و`textDirection: rtl` يضبط النصّ داخل الخليّة لا ترتيب الأعمدة. اعكس الأعمدة بنفسك عبر `rtlRow`/`rtlRows`/`rtlColumnMap`. |
| ❌ لا تُضِف عموداً للحالة بلا حارس يقرؤه | `treasuries.is_active` عاش من أول يوم بمبدّل في الواجهة و**صفر قارئ** — فالمال يدخل خزينةً أوقفها المالك بنفسه. نمط ع-٠٦. |
| ❌ لا تظنّ أن نجاح الاختبارات يعني أن الميزة تعمل | ١٨٥ اختباراً نجحت بينما قاعدة التقاطع معطوبة كلياً — كانت في طبقة العرض. |

التفاصيل الكاملة لكل محذور في [docs/05-LESSONS.md](docs/05-LESSONS.md).

---

## 🏃 تشغيل سريع

```bash
flutter pub get
```
```bash
dart run build_runner build --delete-conflicting-outputs
```
```bash
flutter run -d windows
```

على VS Code: اضغط **F5** — الإعداد الافتراضي هو ويندوز.
التفاصيل الكاملة في [docs/06-SETUP.md](docs/06-SETUP.md).

---

## ▶️ الخطوة التالية — اقرأ هذا القسم أولاً

**الحالة:** ستّ عشرة مرحلة منجزة + دفعتان من ملاحظات المالك الميدانية.
المخطَّط المعتمد وخطة الرواتب **مكتملان**، ولا عطل معروف مفتوح.

### 📦 ما بقي من ملاحظات المالك — دفعتان

أرسل المالك **ستّ عشرة ملاحظة** دفعةً واحدة (2026-08-30) بعد جولة اختبار
موسّعة. رُتِّبت في أربع دفعات، **نُفِّذت أ و ب**، وبقيت:

#### الدفعة ج — التقارير والتصدير
| البند | الحالة اليوم |
|---|---|
| **كروت الدولار في لوحة التحكم** | بطاقات **الخزائن** تعرض الدولار، لكن كروت القبض/الصرف/التحويل والمخططات **بالدينار حصراً** |
| **تفاصيل السلفة في تقرير السلف** | البحث الحالي على **رقم السلفة واسم المشروع فقط**. المطلوب: بحثٌ داخل السلفة بالبند (وقود · كهربائيات · مشتريات…) |
| **تصدير السلفة بتفاصيلها** | التصدير يخرج **المبلغ الكلي** لا سطور السلفة |
| **حفظ كشف الرواتب بصيغة Excel** | غير موجود — والبنية جاهزة (`ExcelExportService`) |

#### الدفعة د — تغيير مخطط (Schema v8) · تحتاج موافقة صريحة
| البند | ما يحتاجه |
|---|---|
| **حالة الموظف**: حالي · منتهية خدمته · إجازة | عمود `status` + ترحيل v7→v8 + فلاتر |
| **الأقسام والترتيب اليدوي** (مهندسون ١–٧ · فنيون ٨–١٩ · سواق ٢٠–٣٥…) | جدول `departments` + `employees.department_id` + `sort_order` + شاشة إدارة أقسام + سحب وإفلات |

⚠️ البند الثاني وحده يقارب حجم **مرحلة كاملة**، وهو تفعيلٌ للقرار المؤجَّل
«ترتيب يدوي للموظفين».

### ❓ سؤالان ينتظران جواب المالك — لا تبدأ بهما بلا إجابة

**١. «خصومات أخري»** — بلّغ المالك أن إيصال الراتب يكتب «خصومات أخري»
والصواب «أخرى». **وبحثُ المصدر كلّه يقول إن النصّ المكتوب صحيح**
(`pdf_payroll_documents.dart:288`). فإمّا الموضع آخر لم يُعثَر عليه، أو
مسألة رسم حروف في عارض الـPDF. **لم يُغيَّر شيء** — تصحيحُ نصٍّ سليم يُفسده،
والمطلوب لقطة أو اسم الشاشة.

**٢. واجهة المقاولين والشركاء** — `treasury_id` يبقى `NULL` **أبداً** لأن
واجهة إنشاء المقاول/الشريك لا تمرّره، و`kind` ثابت `'main'` عند إنشاء
الخزينة. فتبويبات الفلترة «مقاولون/شركاء» وشاراتها **واجهة لبيانات لا يمكن
أن توجد**. و`sharePercentage` بلا أي حساب مالي — شريط عرض فقط.
**السؤال: أتُوصَل الميزة أم تُزال واجهتها؟**

📌 الاثنتا عشرة دالة الخاصة بربط المقاولين/الشركاء بالخزائن **استُثنيت عمداً
من كنس الكود الميت** (المرحلة ١٦د) — حذفها يُغلق باب «وصلها» قبل الجواب.

### ⚙️ قرارات مؤجَّلة أقدم
| البند | ما ينتظر |
|---|---|
| ضغط النسخة الشاملة في ملف واحد | إعلان `archive` صراحةً (القانون ٦). البديل المُنفَّذ: مجلد |
| تشفير المرفقات في النسخة الشاملة | القاعدة مشفَّرة والمرفقات **لا** — مذكور في البيان |

### 📜 المراحل المنجزة
`plan/MASTER_PLAN_PAYROLL.md` يوثّق المراحل ١–١٢ (نظام الرواتب والسلف
وتصفير المصنع). والمراحل **١٣–١٦** ودفعتا الملاحظات موثَّقة تفصيلاً في
[04-STATUS.md](docs/04-STATUS.md) و[PROGRESS_LOG](plan/PROGRESS_LOG.md).

| المرحلة | الموضوع |
|---|---|
| **١٣** | ثلاثة أعطال صامتة (ع-٤١ · ع-٤٢ · ع-٤٣) + حارس نمطي |
| **١٤** | النسخ الاحتياطي والاستعادة الشاملة (+ ع-٤٤) |
| **١٥** | الطباعة والتصدير — التبويبات الثمانية + xlsx مُنسَّق |
| **١٦** | الحُرّاس والدين التقني — سبع صلاحيات · الدولار · كنس الميت |
| **أ** | ثغرات: الخزينة المعطَّلة · حذف السند · سجل التدقيق · اتجاه الجداول |
| **ب** | الموظفون: التعريب · الحذف المحروس · تفاصيل السلفة · النقل · التلوين · الفلتر |

